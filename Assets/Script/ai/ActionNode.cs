public class ActionNode{
	public string id;
	public object param;


	/*ActionNode(string str){
		var arr:Array = str.split(",");
		switch(arr[0]){
			case "Go": param = new Point(Number(arr[1]),Number(arr[2]));break;
			case "Follow": param = Number(arr[1]);break;
			case "Attack": param = arr[1] ? int(arr[1]) : 0;break;
			default: if(arr[1]) param = arr[1];
		}
		id = arr[0];
	}*/
}